import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    admin:null
}


export const adminSlice = createSlice({
    name: 'adminSlice',
    initialState,
    reducers: {
        
    }
})